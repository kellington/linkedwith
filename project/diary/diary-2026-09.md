# 2026-09

## 2026-09-09
- another update - data/Basic_LinkedInDataExport_09-09-2026.zip.zip

Yes. Refresh complete.

- `data/linkedwith.json` — 774 contacts, 10 user-added preserved
- `output/LinkedWith.HTML` — 774 rows embedded, no external deps, offline-ready


## 2026-09-10
- Making sure OK that it is Public
- I found none of your personal data in the repo. I checked every commit on every branch (15 commits, all already on GitHub), not just the current files.

- **Data files:** no ZIP, CSV, JSON store or `LinkedWith.HTML` output has ever been committed. The history starts at "Initial Commit after cleanup".
- **Emails, phones, profile URLs:** every hit is made-up test data (John Doe, Alice Smith, `example.com`, 555 numbers, `linkedin.com/in/johndoe`).
- **Status HTML and `AI+PROCESS`:** only totals (761 and 774 connections, 176 with messages) and export filenames. No names.
- **Diary and ideas notes:** no contact names, only column names and plans.
- **Secrets:** no API keys, tokens or passwords.
- **Protected by `.gitignore`:** `data/`, `output/` and `tests/fixtures/` are all excluded. So are `*.zip`, `*.csv` and `*.html`, except the status pages and `AI+PROCESS.html`.

Minor things you're already sharing:
- Commits show `rob@skyideas.com` as the author email, which is normal for GitHub.
- `SKILL.md:21` includes your local path `/Users/rob/Documents/GitHub/...`, so your macOS username is visible.
- JSON files are only ignored inside `data/`. A `.json` saved anywhere else in the repo wouldn't be blocked. If you want, I can add `*.json` to `.gitignore` as a safeguard.

Nothing needs removing.


## 2026-09-20
- another refresh - data/Complete_LinkedInDataExport_09-19-2026.zip.zip
- Note: in Knowledge, I am working to improve my LinkedIn profile
-
### Session log — refresh from Complete_LinkedInDataExport_09-19-2026

Ran `uv run python linkedwith.py data/Complete_LinkedInDataExport_09-19-2026.zip.zip`.

- Parsed **758** connections from the export; store now **781** total
- **+7 new**; 23 in the store are no longer in the export (kept, not deleted)
- `data/linkedwith.json` updated — user-added email/phone/notes preserved
- `output/LinkedWith.HTML` rebuilt (212 KB, self-contained)
- Facets: 180 messaged · 3 notes · 13 with email or phone

Verified in Chrome (served over localhost — `file://` is blocked for the browser
tooling): cards and table views both render, facet chips and year/sort controls
present, inner table scroll works, header reads "LinkedIn data from 2026-09-19 ·
built 2026-09-20", **no console errors**. Tests: **96/96 passing**.

Open items worth a look next time:
- Pipeline logs ~10 `WARNING: skipping record with no URL` lines — blank rows in
  LinkedIn's CSV. Harmless, but noisy; could be collapsed into one summary line.
- 23 connections have dropped out of the export (unfollows/closed accounts).
  They stay in the store silently — consider surfacing them in the HTML.


Session closed out.

**Repo has no STATE/TASKS/DECISIONS/PLAN files** — it uses `project/diary/` + `project/status/STATUS-SUMMARY.md` instead. I followed that convention rather than creating four new protocol files unasked. Say the word if you want the standard set scaffolded.

**Written**
- `project/diary/diary-2026-09.md` — session log appended under your 2026-09-20 notes: refresh numbers, browser verification, 96/96 tests, two open items
- `project/status/STATUS-SUMMARY.md` — status + `generated` bumped to 2026-09-20, 781 connections / 180 messaged

**Decisions:** none this session — a data refresh, no choices made. Nothing to draft.

**Plan drift:** n/a, no PLAN.md.

**Uncommitted** on branch `vscode`: both files above. Not committed — tell me if you want that.

**Next session:** collapse the ~10 "skipping record with no URL" warnings into one summary line, and decide whether the 23 dropped-out connections should be visible in the HTML rather than sitting silently in the store.

### 2026-09-24 — verification check

Checked whether the store and HTML already reflect the
`Complete_LinkedInDataExport_09-19-2026` refresh. They do. Nothing re-run.

- All 758 export connections that have a URL are in `data/linkedwith.json`.
  After lowercasing the percent-encoded URLs, the 23 dropped-out count matches the 09-20 log.
- The newest connection (2026-09-16) appears in both the JSON and the HTML.
- Possible duplicate: the store has both `steve-dufault-b6a07445` and
  `steve-dufault-cba%c2%ae-b6a07445`. LinkedIn changed the slug, so it's one
  person under two keys. Worth a look along with the dropped-out connections.
